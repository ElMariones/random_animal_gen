# Source design references & raw data (not deployed)
design/
animals.txt
._animals.txt

# OS / editor cruft
.DS_Store
._*
*.log

# deps (none used, but just in case)
node_modules/
.vercel

# Local config with Cloudinary + Supabase keys — kept out of the repo.
# Copy config.example.js to config.js and fill in your own values.
config.js
